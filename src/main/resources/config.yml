debug: false

# Changing these values requires a restart
# How many seconds between backups
backup-duration: 600
initial-backup-duration: 600

session:
  # How many seconds a player must be online to earn a reward
  threshold: 1800
  # Placeholders are {player}, {player_uuid}, {world}
  rewards:
  - "cc give p dailyrewardcrate 5 {player}"
  reward-message: "The Oracle marks your presence. Your /daily-reward is delivered."
  notify-message: "Half the span has passed. Daily reward in %d minutes."

community-rewards-enabled: false

# Posts a daily roster to Discord at the day-change reset (0:00 UTC), listing
# everyone who logged in that day. It works by dispatching a console command, so
# it integrates with any Discord bridge that exposes one (DiscordSRV, EssentialsX
# Discord, etc.) -- just point `command` at the right syntax. The announcement
# fires right BEFORE sessions reset, so the roster is always complete.
discord-announce:
  # Master switch. Leave false unless a Discord bridge plugin is installed and
  # the command below is correct, otherwise the dispatch simply fails (and logs a
  # warning) at every reset.
  enabled: false
  # The Discord channel the announcement is posted to. Substituted for {channel}
  # in the command below -- use whatever your bridge expects (a channel name, an
  # id, or a #mention).
  channel: "general"
  # Console command dispatched to post the announcement.
  # Placeholders: {channel}, {message}
  command: "discord announce {channel} {message}"
  # The announcement body. Placeholders: {players} (comma-separated names),
  # {count} (how many played), {date} (the day being summarized).
  message: "{count} players logged in on {date}: {players}"
  # Posted instead when nobody logged in that day. Leave empty to post nothing on
  # empty days.
  empty-message: "No players logged in on {date}."

# Community sell-multiplier bonus, granted via LuckPerms when the server hits a
# playtime goal so every online player benefits.
community-bonus:
  # LuckPerms group the bonus permission is granted to.
  group: "default"
  # Safety-net duration only. The authoritative terminator is the daily reset
  # (DayChange), which revokes the bonus on the day change. This duration only
  # guarantees a granted bonus never expires BEFORE the next reset, which always
  # arrives within 24h. Keep it comfortably longer than a day (e.g. 2d) -- do NOT
  # shorten it below 24h or a bonus could lapse mid-day.
  duration: "2d"
  # Show a boss bar to every online player while a community sell-hand bonus is
  # active. It displays the current boost and nudges players to /daily-reward for
  # the full picture. Only ever shown when community-rewards-enabled is true and a
  # tier has been unlocked; it clears automatically at the daily reset. Individual
  # players can hide it for themselves with /daily-reward bossbar off.
  boss-bar: true

# How many seconds between sending daily-reward message and streak message
streak-delay: 3

# Placeholders are {player}, {player_uuid}, {world}, {streak}
streaks:
  enabled: false
  encouragement-message: "Earn your /daily-reward again tomorrow for a streak reward!"
  # no rewards for streaks less than 2
  rewards:
    2:
    - "cc give p dailyrewardcrate 1 {player}"
    3:
    - "cc give p dailyrewardcrate 2 {player}"
    7:
    - "cc give p dailyrewardcrate 3 {player}"
    14:
    - "cc give p dailyrewardcrate 5 {player}"
    30:
    - "cc give p dailyrewardcrate 10 {player}"

  # Placeholder is %d for streak count
  reward-messages:
  - "The Oracle observes your return. %d days sustained. A reward forms in your hands."
  - "Your pattern persists. %d days aligned with Theatria. The Oracle grants you this boon."
  - "The currents shift around you. %d days endured. The Oracle releases a reward."
  - "Constancy is rare. You carry it. %d-day streak confirmed. The Oracle responds."
  - "You walk the same path again. %d days straight. The Oracle honors your persistence."
  - "Your presence stabilizes the old mechanisms. %d days. The Oracle answers."
  - "Another day returned. %d days in sequence. The Oracle rewards your devotion."
  - "Theatria bends around your rhythm. %d days secured. The Oracle grants its gift."
  - "Your name echoes in the Oracle’s chamber. %d days. A reward is manifested."
  - "You resonate with the world again. %d days in alignment. The Oracle acknowledges you."